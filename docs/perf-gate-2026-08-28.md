# 性能门槛 2026-08-28(占位说明)

> 状态:占位。仓库历史重置后,原文已不在 `git` 历史中。保留此文件是为了让 `perf/README.md`、
> `perf/driver.js`、`perf/editor-load.cjs`、`perf/thresholds.json`、`e2e/editor.cjs`、
> `e2e/README.md`、`e2e/spec.js` 中的引用不悬空。

原文是 2026-08-28 的性能门槛设计与结论文档,被多处引用为以下结论的依据:

- §6:文本面从 `<textarea>` 换成 CodeMirror 6 的内存动机。
- §9 #4:打开首个标签的一次性成本,以及 `openFirst` / `openSteady` 拆分的由来(差点因此撤回代码分割)。
- §15:fresh-profile 修复与第九个夹具文件(`expandable.ts`)的来历。
- 指标定义:`rendererBaselineMiB` / `loadDeltaMiB` / `inlineDeltaMiB` / `retainedDeltaMiB` 等判定线。

当前门槛的可执行定义以 `perf/thresholds.json` + `perf/editor-load.cjs` 为准;原始论证可参考
`perf/README.md` 与本仓库 `docs/` 下其他设计文档。本占位文件不替代原文内容。
