# 修复核验报告 2026-08-24(占位说明)

> 状态:占位。仓库历史重置后,原文已不在 `git` 历史中。保留此文件是为了让 `perf/README.md` 的引用不悬空。

原文是 P2-004 架构修复(只挂载活动编辑器查看器)的核验报告,`perf/README.md` 引用其
"残余风险第 4 条"作为性能门槛存在的原因。原文结论的要点:

- 修复后 8 个标签打开时页面上的查看器数量为 1,架构不变量成立;
- 但性能症状本身没有复测:没有数字、没有阈值,于是"性能问题是否关闭"无法判定;
  因此需要把负载、指标和判定线固定成可重复执行的一条命令,即 `npm run perf:editor`。

可执行门槛以 `perf/thresholds.json` 与 `perf/README.md` 为准,`perf/last-run.json` 记录每次运行结果。
本占位文件不替代原文内容。
